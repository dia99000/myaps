<div class="form">
	<%= form_with(model: card, local: true) do |form| %>
		<div class="field">
			<%= form.label :タイトル %>
			<%= form.text_field :title %>
		</div>
		<div class="actions">
			<%= form.submit %>
		</div>
	<% end %>
</div>