<%= form_with(model: card, local: true) do |form| %>
	<div class="field">
		<%= form.text_field :title,:placeholder => "カードを追加..." %>
		<%= button_tag :type => "submit" do %>
			<i class="fa fa-plus"></i>
		<% end %>
	</div>
<% end %>